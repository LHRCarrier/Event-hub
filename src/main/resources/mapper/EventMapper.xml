<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bubbles.eventhub.mapper.EventMapper">

    <sql id="baseColumns">
        event_id, name, date, location, description, category_id, community_id, status, create_time, update_time
    </sql>

    <resultMap id="EventResultMap" type="com.bubbles.eventhub.entity.Event">
        <id column="event_id" property="eventId" />
        <result column="name" property="name" />
        <result column="date" property="date" />
        <result column="location" property="location" />
        <result column="description" property="description" />
        <result column="category_id" property="categoryId" />
        <result column="community_id" property="communityId" />
        <result column="status" property="status" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>

    <resultMap id="EventWithCategoryResultMap" type="com.bubbles.eventhub.entity.Event" extends="EventResultMap">
        <result column="category_name" property="categoryName" />
    </resultMap>

    <select id="selectByStatus" resultMap="EventResultMap">
        SELECT <include refid="baseColumns"/>
        FROM events
        WHERE status = #{status}
        ORDER BY date ASC
    </select>

    <select id="selectUpcomingEvents" resultMap="EventResultMap">
        SELECT <include refid="baseColumns"/>
        FROM events
        WHERE date > NOW()
        AND status = 'ACTIVE'
        ORDER BY date ASC
    </select>

    <select id="selectAllWithCategory" resultMap="EventWithCategoryResultMap">
        SELECT e.*, c.name AS category_name
        FROM events e
        LEFT JOIN categories c ON e.category_id = c.category_id
        ORDER BY e.date DESC
    </select>

    <select id="searchEvents" resultMap="EventResultMap">
        SELECT <include refid="baseColumns"/>
        FROM events
        <where>
            status = 'ACTIVE'
            <if test="keyword != null and keyword != ''">
                AND (name LIKE CONCAT('%', #{keyword}, '%') OR description LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="categoryId != null">
                AND category_id = #{categoryId}
            </if>
        </where>
        ORDER BY date ASC
    </select>

    <select id="selectByCommunity" resultMap="EventResultMap">
        SELECT <include refid="baseColumns"/>
        FROM events
        <where>
            <if test="communityId != null">
                community_id = #{communityId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY date ASC
    </select>

    <update id="updateById">
        UPDATE events
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="date != null">date = #{date},</if>
            <if test="location != null">location = #{location},</if>
            <if test="description != null">description = #{description},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="communityId != null">community_id = #{communityId},</if>
            <if test="status != null">status = #{status},</if>
            update_time = NOW()
        </set>
        WHERE event_id = #{eventId}
    </update>

    <delete id="deleteById">
        DELETE FROM events WHERE event_id = #{eventId}
    </delete>

</mapper>
