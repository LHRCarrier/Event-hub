<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bubbles.eventhub.mapper.CommunityApplicationMapper">
    <sql id="baseColumns">application_id, community_id, user_id, message, status, apply_time, approve_time, approve_by, reject_reason</sql>
    <select id="selectByCommunityAndStatus" resultType="com.bubbles.eventhub.entity.CommunityApplication">
        SELECT <include refid="baseColumns"/> FROM community_applications WHERE community_id = #{communityId} AND status = #{status} ORDER BY apply_time DESC
    </select>
    <select id="selectByUserId" resultType="com.bubbles.eventhub.entity.CommunityApplication">
        SELECT <include refid="baseColumns"/> FROM community_applications WHERE user_id = #{userId} ORDER BY apply_time DESC
    </select>
    <select id="countPendingApplications" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM community_applications WHERE community_id = #{communityId} AND user_id = #{userId} AND status = 'PENDING'
    </select>
    <select id="selectPageByCommunityAndStatus" resultType="com.bubbles.eventhub.entity.CommunityApplication">
        SELECT <include refid="baseColumns"/> FROM community_applications
        <where>community_id = #{communityId}<if test="status != null and status != ''">AND status = #{status}</if></where>
        ORDER BY apply_time DESC
    </select>
    <select id="selectPageByUserId" resultType="com.bubbles.eventhub.entity.CommunityApplication">
        SELECT <include refid="baseColumns"/> FROM community_applications WHERE user_id = #{userId} ORDER BY apply_time DESC
    </select>
    <select id="countPendingApplicationsByCommunity" resultType="java.lang.Integer">
        SELECT COUNT(*) FROM community_applications WHERE community_id = #{communityId} AND status = 'PENDING'
    </select>
    <update id="updateById">
        UPDATE community_applications
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="approveTime != null">approve_time = #{approveTime},</if>
            <if test="approveBy != null">approve_by = #{approveBy},</if>
            <if test="rejectReason != null">reject_reason = #{rejectReason},</if>
        </set>
        WHERE application_id = #{applicationId}
    </update>
</mapper>